import { describe, expect, it } from 'vitest';
import SongDetailsPanels from './SongDetailsPanels.svelte';

describe('SongDetailsPanels', () => {
	it('should be a valid Svelte component', () => {
		expect(SongDetailsPanels).toBeTruthy();
	});
});
