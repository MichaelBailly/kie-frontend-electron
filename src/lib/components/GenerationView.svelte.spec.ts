import { describe, expect, it } from 'vitest';
import GenerationView from './GenerationView.svelte';

describe('GenerationView', () => {
	it('should be a valid Svelte component', () => {
		expect(GenerationView).toBeTruthy();
	});
});
